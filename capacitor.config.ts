import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.multivision.network',
  appName: 'Multivision Network',
  webDir: 'out'
};

export default config;
